# 第一个发行版 v1.0.0

2026-09-12

相机拍完自动进手机、自动传网盘的工具。这个版本完成了从相机到网盘的完整链路，在尼康 Z6 上日常使用验证通过。

## 功能清单

**相机连接**

- 品牌选择：自动 / 尼康 / 索尼（底部抽屉菜单，选择会记住）
- USB 数据线连接（相机 USB 模式设为 PTP/MTP）
- WiFi 热点连接（手机连相机热点，无网也能传）
- 连接过程有浮层提示，失败时直接显示原因并可一键重试
- 连接成功后显示相机真实型号、电量、存储卡余量

**拉图与传输**

- 三列缩略图浏览，RAW 文件有角标，按拍摄时间倒序
- 缩略图按需加载（只请求屏幕附近的图），已拉取过的直接读本地，秒开
- 点选任意张 → 悬浮面板全选/批量拉取，实时进度
- 拍照后新图自动拉到手机（尼康走事件推送，索尼走轮询）
- 拉取的图先落本地，再按策略进「已拉取」列表，与系统相册文件夹一一对应

**上传**

- 传输页三个子页：正在拉取 / 已拉取 / 上传进度
- 已拉取页多选批量上传到自建 Rust 网盘
- 上传策略三档：仅存本地 / WiFi 环境上传 / 立即上传
- 按拍摄日期在网盘自动建目录，断网自动挂起、恢复后续传，401 自动重登
- 条件保护：仅充电时上传、低电量暂停

**后台运行**

- 前台服务保活，锁屏或退到桌面后继续自动拉取和上传
- 通知栏常驻显示状态（相机已连接 / 几个文件传输中），可关闭

## 支持设备

| 设备 | USB | WiFi | 备注 |
|---|---|---|---|
| 尼康 Z6 | ✅ 已验证 | ✅ 已验证 | 唯一完整测试过的机型 |
| 其他尼康 PTP 机型 | 理论可用 | 未验证 | 欢迎反馈 |
| 索尼 α 系列（MTP 模式） | 理论可用 | — | 标准协议，未经实机验证 |
| 索尼旧世代（发片显示 SSID+密码） | — | 协议已实现 | 未真机验证 |
| 索尼新世代（扫码配对） | 可用 | ❌ 不支持 | 加密配对，请用 USB |

## 已知限制

- WiFi 模式下相机热点没有互联网，所以采用本地优先：先落图，后台服务再择机上传，两条线路并行
- 相机同时只允许一个客户端，连接前请关掉 SnapBridge / Creators' App 等官方应用
- 索尼 WiFi 拿不到原始 RAW（相机只给转出的 JPEG，官方行为），要 RAW 请用 USB
- 国产系统（ColorOS 等）后台管控较严，建议锁定后台并加电池白名单
- 同一文件同一时间只有一个拉取任务，重复触发自动跳过

## 安装

方式一：下载 Releases 里的 APK，直接安装（需要 Android 10+）。

方式二：自行构建

```bash
flutter pub get
flutter build apk --release
```

依赖 Flutter 3.35 和 JDK 17，无需任何厂商 SDK 或密钥。

## 快速上手

1. 设置 → 私有网盘，填入网盘地址、账号、密码并登录
2. 相机页选好品牌，点 USB 或 WiFi 连接
3. 设置 → 拉取策略，勾选要自动拉取的文件类型（默认 JPEG + NEF）
4. 设置 → 上传策略，选「WiFi 环境上传」或「立即上传」
5. 相机页连接成功后正常拍照，新图会自动出现在手机相册「AkihanaMiao」文件夹并按策略上传

## 下一步

佳能、松下驱动（驱动抽象已就位，接入成本低），iOS 与鸿蒙适配（iOS 计划 WiFi-only）。路线图见 `BUILD_SPEC.md`。
